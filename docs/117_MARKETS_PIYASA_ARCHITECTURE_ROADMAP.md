# Markets / Piyasa — Architecture Roadmap

1. **Additive** — header widget only; ERP cash/GL unchanged.
2. **UI** — `HeaderMarketRates` in `app-header-banner` (left of Destek).
3. **Data** — `src/markets/localStore.js` mock → later backend provider.

No dedicated `/piyasa` route until a full markets module is approved.
