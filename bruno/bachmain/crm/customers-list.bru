meta {
  name: Customers list — CRUD read
  type: http
  seq: 1
}

get {
  url: {{apiBase}}/v1/customers
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: in [200, 401, 403]
}
