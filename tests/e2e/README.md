# Playwright E2E

Projects:

- `web-chromium` — login, register, forgot password, demo, package entry
- `app-chromium` — CRM dashboard + process modules
- `admin-chromium` — yönetim panel

Reports: `tests/reports/playwright/html` (failure traces/screenshots/videos retained).
